# Copy to .streamlit/secrets.toml for local runs (this example file is safe to commit;
# the real secrets.toml is git-ignored). On Streamlit Cloud, set this in the
# app's "Secrets" settings instead.
GEMINI_API_KEY = "your-gemini-api-key-here"
